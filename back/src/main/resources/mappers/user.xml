<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.jwt_test.model.mapper.UserMapper">
    <insert id="insertUser" parameterType="User">
        insert into user(username, email, gender, join_at, roles)
        values (#{userName}, #{email}, #{gender}, #{joinAt}, #{roles})
    </insert>
    <select id="searchByUserName" parameterType="String" resultType="User">
        select * from user
        where username = #{userName}
    </select>
</mapper>